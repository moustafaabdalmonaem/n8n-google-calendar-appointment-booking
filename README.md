# n8n-google-calendar-appointment-booking
An n8n workflow that automates appointment booking: checks Google Calendar for conflicts in real time, then auto-confirms or rejects the request via email — no manual scheduling needed.
